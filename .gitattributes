# Normalize line endings to LF in the repo regardless of which OS/editor
# touched a file -- this project is developed on both Windows and Linux.
# PowerShell (5.1+ and 7+) handles LF scripts fine, so no CRLF carve-out
# needed for .ps1 files.
* text=auto eol=lf
